const int POT_MIN = 0;
const int POT_MAX = 1024;
RGBPot sliders[Zone_Count] = {
  RGBPot(0*3, POT_MIN, POT_MAX),
  RGBPot(1*3, POT_MIN, POT_MAX),
  RGBPot(2*3, POT_MIN, POT_MAX),
  RGBPot(3*3, POT_MIN, POT_MAX),
  };
